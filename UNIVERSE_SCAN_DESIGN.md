# تصميم مسح العملات — Video FVG/Fibonacci Bot

## مصدر الفكرة

تم أخذ جزء اكتشاف العملات فقط من الكود المرفق: جلب `fetch_tickers()`، فلترة عقود USDT، استبعاد الرموز غير النشطة، ترتيب العقود حسب `quoteVolume`، واستخدام حد أعلى للعدد مع حد أدنى للسيولة. لم يتم نقل استراتيجية Golden Key أو تقاطع EMA أو أهدافها إلى البوت.

## قواعد الاختيار

يعمل البوت افتراضياً على العقود الدائمة الخطية ذات التسعير USDT فقط. يجب أن تكون السوق نشطة، وأن تعيد بيانات ticker قيمة `quoteVolume` لا تقل عن `VIDEO_MIN_24H_VOLUME`، ثم تُرتب العملات تنازلياً حسب الحجم. الإعداد الافتراضي يفحص أعلى 300 عقد مؤهل، ويمكن وضع `VIDEO_TOP_COINS_LIMIT=0` لفحص كل العقود المؤهلة، لكن ذلك يزيد عدد طلبات OHLCV واحتمال الاصطدام بحدود API.

## دورة المسح

في كل دورة، يجلب البوت tickers جماعياً مرة واحدة، يختار قائمة الرموز، ثم يجلب 1D و4H و1H لكل رمز بحد أقصى للتوازي. تُستخدم الشموع المغلقة فقط. بعد ذلك يطبق محرك Video FVG/Fibonacci على كل رمز، يجمع الإشارات الصالحة، ويختار الأعلى ثقة ثم الأعلى Net RR. إذا لم توجد إشارة مكتملة، لا يرسل Telegram رسالة.

## الحماية

يظل هناك رمز نشط واحد فقط، وفترة تهدئة بعد الإشارة، مع حفظ حالة الصفقة. لا تُستخدم دوال تنفيذ أوامر؛ التفعيل والهدف والوقف كلها Paper Trading. رمز `VIDEO_SYMBOL` يُستخدم فقط عندما تكون `VIDEO_SCAN_UNIVERSE=false`.
