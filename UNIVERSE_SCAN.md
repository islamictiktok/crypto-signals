# مسح جميع العملات في بوت Video FVG/Fibonacci

أصبح البوت لا يعتمد على `VIDEO_SYMBOL` وحده. عند `VIDEO_SCAN_UNIVERSE=true` يجلب tickers المنصة جماعياً، ثم يبحث في الأسواق النشطة التي تحقق الشروط التالية: عقد Swap خطي، التسعير USDT، و`quoteVolume` يومي لا يقل عن `VIDEO_MIN_24H_VOLUME`.

بعد التصفية تُرتب الرموز حسب حجم التداول، ويفحص البوت أعلى `VIDEO_TOP_COINS_LIMIT` رمزاً. القيمة الافتراضية 300، وهي نفس فكرة الكود المرفق. إذا وضعت القيمة `0` فسيُفحص كل عقد USDT الخطي المؤهل، لكن ذلك قد يرفع عدد طلبات OHLCV ويحتاج إلى مراعاة حدود API.

لكل رمز مؤهل، يجلب البوت الشموع المغلقة على 1D و4H و1H، ثم يطبق استراتيجية Video FVG/Fibonacci للشراء والبيع. لا تُرسل أي رسالة لمجرد أن العملة ذات حجم كبير؛ يجب أن تكتمل القناة، والدعم/المقاومة، وOrder Block، وFVG، وFibonacci، وأن تتجاوز الصفقة حد Net RR. عند وجود عدة فرص، يختار البوت أعلى ثقة ثم أعلى Net RR، ويحافظ على صفقة ورقية نشطة واحدة فقط.

## الإعدادات

| المتغير | القيمة الافتراضية | الوظيفة |
|---|---:|---|
| `VIDEO_SCAN_UNIVERSE` | `true` | تشغيل مسح المنصة بدلاً من رمز واحد. |
| `VIDEO_TOP_COINS_LIMIT` | `300` | أقصى عدد من الرموز بعد ترتيبها بالحجم؛ `0` يعني بلا حد. |
| `VIDEO_MIN_24H_VOLUME` | `5000000` | الحد الأدنى لحجم التداول المقوم بالـ USDT. |
| `VIDEO_SCAN_CONCURRENCY` | `4` | عدد طلبات تحليل الرموز المتزامنة. |
| `VIDEO_POLL_SEC` | `300` | الفاصل بين دورات المسح؛ لأن الاستراتيجية تستخدم 1D/4H/1H. |
| `VIDEO_SYMBOL` | `BTC/USDT:USDT` | يُستخدم فقط عند تعطيل مسح المنصة. |

## ملاحظة تشغيلية

فحص كل 300 عقد عبر ثلاثة أطر زمنية قد يستغرق وقتاً ويستهلك حدود API. لذلك صُمم المسح ليجلب tickers مرة واحدة، ويستخدم التوازي المحدود، ويحتفظ بكاش لكل رمز. لا توجد في النسخة أي دوال تنفيذ أوامر حقيقية، وكل الإشارات والتفعيلات والنتائج ورقية.

## مصدر الجزء المضاف

الجزء المضاف مأخوذ من فكرة البحث في الكود المرفق: `fetch_tickers()`، فلترة USDT والحجم، ترتيب العملات، ثم تحليل الرموز. لم يتم نقل استراتيجية EMA أو Golden Key أو إدارة الرافعة من الكود المرفق.
